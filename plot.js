(()=>{const data=window.plotArchive;if(!data)return;const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const tabs=document.querySelector('#plot-tabs'),meetings=document.querySelector('#plot-meetings'),chapters=document.querySelector('#plot-chapters'),clues=document.querySelector('#plot-clues');function select(i){tabs.querySelectorAll('button').forEach((b,j)=>b.classList.toggle('active',i===j));const u=data.units[i];meetings.innerHTML=u.rounds.map((r,j)=>`<article class="plot-meeting searchable"><span>${u.name} / MEETING ${j+1} / 90 MIN</span><h3>${esc(r[0])}</h3><p>${esc(r[1])}</p><ul>${r[2].map(x=>`<li>${esc(x)}</li>`).join('')}</ul></article>`).join('')}tabs.innerHTML=data.units.map((u,i)=>`<button class="${i?'':'active'}" data-i="${i}">${esc(u.name)}</button>`).join('');tabs.addEventListener('click',e=>{const b=e.target.closest('button');if(b)select(+b.dataset.i)});chapters.innerHTML=data.chapters.map(c=>`<article class="plot-chapter searchable"><button><span>${esc(c[0])}</span><b>${esc(c[1])}</b><small>${esc(c[2])}</small></button><div class="plot-scenes">${c[3].map(x=>`<div>${esc(x)}</div>`).join('')}</div></article>`).join('');chapters.addEventListener('click',e=>{const b=e.target.closest('button');if(b)b.parentElement.classList.toggle('open')});clues.innerHTML=`<h2>考察・伏線管理</h2><div class="matrix-wrap"><table><thead><tr><th>要素</th><th>初出</th><th>再提示・回収</th><th>残す余白</th></tr></thead><tbody>${data.clues.map(r=>`<tr>${r.map(x=>`<td>${esc(x)}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;select(0)})();
